node_modules/
dist/
out/
.DS_Store
*.log
.agentdeck-worktrees/

# secrets / local env (never commit)
.env
.env.*
!.env.example

# Claude Code agent state + local settings (runtime, not project content)
.claude/

# scratch handoff notes (throwaway, never commit)
HANDOFF.md

# generated icon intermediates (build/icon.icns IS committed)
build/icon.iconset/
build/icon_master.png
